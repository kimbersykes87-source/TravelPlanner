# Travel Planner v2 – environment variables
# Copy to .env.local for local development
# For Cloudflare Pages: set in Dashboard → Settings → Environment variables

# Supabase (required for data)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: Web Push public key (see docs/setup/PUSH_NOTIFICATIONS_SETUP.md)
# VITE_VAPID_PUBLIC_KEY=

# Never put the service_role key or database password in a VITE_ variable:
# anything starting with VITE_ is shipped to every visitor's browser.
